[project]
name = "focus-tracker"
version = "1.0.0"
description = "Eye tracking and computer activity monitor to measure concentration"
requires-python = ">=3.10"

[project.scripts]
focus-tracker = "focus_tracker.main:main"
